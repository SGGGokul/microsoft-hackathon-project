# Computer Vision (Sonali)

### What my part does
Runs a minimal inference pipeline using OpenCV and ONNX model.

### How to run locally
python cv/inference.py --input data/samples/image1.jpg --output results/output.json

### How to deploy in Azure
Deploy as an Azure Function (HTTP Trigger) and connect to Storage + API endpoint.
